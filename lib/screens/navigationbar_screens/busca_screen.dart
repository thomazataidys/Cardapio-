// busca_screen.dart
import 'package:flutter/material.dart';

class BuscaScreen extends StatelessWidget {
  const BuscaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Tela de Busca', style: TextStyle(fontSize: 24)),
    );
  }
}
